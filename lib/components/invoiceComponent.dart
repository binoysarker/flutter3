import 'package:flutter/material.dart';

class InvoiceComponent extends StatelessWidget {
  const InvoiceComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [

        ],
      ),
    );
  }
}
